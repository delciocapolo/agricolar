import styled from "@emotion/styled";

const FooterMain = () => {
    const FooterMainContainer = styled['div']``;

    return (
        <FooterMainContainer>

        </FooterMainContainer>
    );
};

export default FooterMain;